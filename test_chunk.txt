use alloc::string::String;
use alloc::vec::Vec;
use consus_core::{Error, Result};

pub fn level_bit_width(max_level: i32) -> u8 {
    if max_level == 0 { 0 } else {
        (32 - (max_level as u32).leading_zeros()) as u8
    }
}